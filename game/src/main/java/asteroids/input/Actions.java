package asteroids.input;

/**
 * This enum is used to track actions which need to be performed.
 */
public enum Actions {
    ACCELERATE,
    DECELERATE,
    ROTATE_LEFT,
    ROTATE_RIGHT,
    SHOOT
}
